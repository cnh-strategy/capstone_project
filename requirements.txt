# Core ML/AI packages
torch>=2.0.0
# tensorflow>=2.13.0  # FinBERT uses PyTorch (현재 미사용)
numpy>=1.21.0,<2.0.0  # Numpy 2.0 호환성 이슈 방지
pandas>=1.3.0
scikit-learn>=1.0.0
lightgbm>=4.0.0
shap>=0.45.0          # TechnicalAgent XAI 기능용

# Data fetching and financial data
yfinance>=0.2.40      # Yahoo Finance API 클라이언트 (최신 버전 권장)

# API and HTTP requests
requests>=2.25.0

# Environment variables
python-dotenv>=0.19.0

# Data serialization
joblib>=1.0.0

# Dashboard and visualization
streamlit>=1.28.0     # 대시보드 (선택사항)
plotly>=5.0.0
matplotlib>=3.5.0     # 백테스트 차트 생성용

# LLM and AI packages
openai>=1.0.0         # GPT 모델 API
anthropic>=0.3.0      # Claude 모델 API (선택사항)
transformers>=4.30.0  # FinBERT 및 기타 트랜스포머 모델

# Data processing & Utils
openpyxl>=3.0.0       # Excel 파일 처리
xlrd>=2.0.0           # Excel 파일 읽기
tqdm>=4.62.0          # 진행률 표시
pyyaml>=6.0           # 설정 파일 처리
scipy>=1.7.0          # 과학 계산
statsmodels>=0.13.0   # 통계 모델링
packaging>=20.0       # 버전 관리

# Web scraping (optional but kept for compatibility)
beautifulsoup4>=4.9.0
newspaper3k>=0.2.8    # 뉴스 기사 추출
lxml>=4.9.0           # beautifulsoup4 파서용

# Development
pytest>=6.0.0         # 테스트 프레임워크
jupyter>=1.0.0        # 주피터 노트북
ipykernel>=6.0.0      # 주피터 노트북 실행용
