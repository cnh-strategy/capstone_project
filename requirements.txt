# Core ML/AI packages
torch>=2.0.0
# tensorflow>=2.13.0  # FinBERT uses PyTorch
numpy>=1.21.0,<2.0.0  # Numpy 2.0 호환성 이슈 방지
pandas>=1.3.0
scikit-learn>=1.0.0
lightgbm>=4.0.0
shap>=0.45.0          # TechnicalAgent XAI 기능용 (필수 추가)

# Data fetching and financial data
yfinance>=0.2.40      # yfinance는 최신 버전 권장 (야후 파이낸스 API 변경 대응)

# API and HTTP requests
requests>=2.25.0

# Environment variables
python-dotenv>=0.19.0

# Data serialization
joblib>=1.0.0

# Dashboard and visualization
streamlit>=1.28.0
plotly>=5.0.0
matplotlib>=3.5.0

# LLM and AI packages
openai>=1.0.0
anthropic>=0.3.0
transformers>=4.30.0

# Data processing & Utils
openpyxl>=3.0.0
xlrd>=2.0.0
tqdm>=4.62.0
pyyaml>=6.0
scipy>=1.7.0
statsmodels>=0.13.0
packaging>=20.0

# Web scraping (optional but kept for compatibility)
beautifulsoup4>=4.9.0
newspaper3k>=0.2.8
lxml>=4.9.0           # beautifulsoup4 파서용

# Development
pytest>=6.0.0
jupyter>=1.0.0
ipykernel>=6.0.0      # 주피터 노트북 실행용
